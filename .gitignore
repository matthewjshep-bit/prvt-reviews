node_modules/
dist/
.env
uploads/
*.log
.DS_Store

# Card Studio local dev fallbacks (Postgres/R2 not configured)
ghl-broker/data/
cardgen/files/

# One-off script output — retag CSVs and the like carry real contact data
ghl-broker/tmp/
